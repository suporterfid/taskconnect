# MVP P0/P1 Implementation Plan

> **For agentic workers:** Execute task-by-task. Prefer TDD for backend behavior. Commit after each coherent unit.

**Goal:** Make pending/manual/test/retry runs execute via cron, fix auth holes, wire failure email, enforce archived envs, and deliver the operator SPA workflow needed for MVP acceptance §28.1–§28.4.

**Architecture:** Extend MySQL claim leases with `PendingRunClaimer`; keep Application orchestration; fix SPA contracts then fill missing pages/actions against existing `/api/v1` routes.

**Tech Stack:** Laravel 12, Vue 3 + TypeScript, PHPUnit, Vitest, Docker via `./scripts/tc.sh`.

## Global Constraints

- No Redis/queue brokers; cron + claim leases only.
- Eloquent models stay under `app/Infrastructure/Persistence/Eloquent/`.
- Outbound HTTP must keep SSRF + DNS pinning.
- i18n: no new user-facing hardcoded English strings.
- Branch: `cursor/mvp-p0-p1-722a`.

---

### Task 1: PendingRunClaimer + cycle wiring

**Files:**
- Create: `app/Application/Scheduling/PendingRunClaimer.php`
- Modify: `app/Application/Scheduling/SchedulerCycleRunner.php`
- Modify: `app/Providers/AppServiceProvider.php`
- Test: `tests/Feature/Scheduling/PendingRunExecutionTest.php`

**Steps:**
1. Write failing feature test: `queueManualRun` → `PendingRunClaimer::claim` → `AttemptExecutor` → `run_state=succeeded`.
2. Implement claimer (lock pending runs, claim pending attempt, return `ClaimedAttempt`).
3. Call claimer from `executeDue()` after due tasks; record heartbeat counts.
4. Commit.

### Task 2: Authorization + API key task scopes

**Files:**
- Modify: `app/Policies/TaskPolicy.php`, `TaskRunPolicy.php`
- Modify: `app/Policies/Concerns/InteractsWithTenantAccess.php`
- Modify: `app/Application/ApiKeys/ApiKeyService.php`
- Test: `tests/Feature/Phase1/TaskAuthorizationTest.php`

### Task 3: FailureNotifier wiring

**Files:**
- Modify: `app/Application/Scheduling/AttemptExecutor.php`
- Test: extend pending/scheduled execution test with `Mail::fake()`

### Task 4: Archived environment guard

**Files:**
- Create: `app/Application/Tenancy/EnvironmentGuard.php` (or method on existing service)
- Modify: Task / Secret / EndpointProfile / ApiKey controllers as needed
- Test: `tests/Feature/Phase1/ArchivedEnvironmentGuardTest.php`

### Task 5: Frontend contracts foundation

**Files:**
- Modify: `frontend/src/services/types.ts`, `api.ts`
- Modify: `frontend/src/composables/useAsyncData.ts`
- Test: Vitest for error parsing / locale if practical

### Task 6: Secrets page + nav

**Files:**
- Create: `frontend/src/pages/SecretsPage.vue`
- Modify: router, AppLayout, i18n en/pt-BR

### Task 7: Task list/detail/wizard operator UI

**Files:**
- Modify: `TaskListPage.vue`, `TaskDetailPage.vue`, `TaskWizardPage.vue`, i18n

### Task 8: Run list/detail + dashboard + platform health

**Files:**
- Modify: `RunListPage.vue`, `RunDetailPage.vue`, `DashboardPage.vue`, `PlatformHealthPage.vue`, i18n

### Task 9: Password reset + preferences + audit + retention

**Files:**
- Backend: preferences controller/route, audit logs controller/route, `RetentionCleaner.php`
- Frontend: Login link, Forgot/Reset pages, SettingsPage, AuditLogsPage

### Task 10: Verify, push, PR

Run backend/frontend tests via `tc` when Docker available; commit/push; open draft PR.
