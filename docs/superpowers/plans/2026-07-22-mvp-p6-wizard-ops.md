# MVP P6 Implementation Plan

**Branch:** `cursor/mvp-p6-wizard-ops-722a`

1. Task list filter/sort API + tests + TaskListPage UI
2. Wizard steps: Schedule | Retry | Test | Review; headers; profile test
3. A11y focus ring + axe smoke + nav label i18n
4. Dependency audit doc + hard-coded string cleanup
5. Manual acceptance checklist + CHANGELOG + PR
