# CI overrides: keep services internal, avoid host port collisions.
services:
  app:
    ports: !override
      - "8080:80"

  mysql:
    ports: !override []

  mailpit:
    ports: !override []

  receiver:
    ports: !override
      - "8090:8080"

  node:
    ports: !override
      - "5173:5173"
